class TasksController < ApplicationController
    def index
        puts "Controller index(tasks) is work!"
    end

    def create
        puts "Controller create(tasks) is work!"
    end

    def show
        puts "Controller show(tasks) is work!"
    end

    def update
        puts "Controller update(tasks) is work!"
    end

    def destroy
        puts "Controller destroy(tasks) is work!"
    end
end
