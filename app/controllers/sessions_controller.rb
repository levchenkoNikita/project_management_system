class SessionsController < ApplicationController
    def login
        puts "Controller login(sessions) is work!"
    end

    def logout
        puts "Controller logout(sessions) is work!"
    end
end
